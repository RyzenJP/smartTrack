<?xml version="1.0"?>
<ruleset name="SmartTrack Coding Standards">
    <description>PSR-12 Coding Standards for SmartTrack Project</description>
    
    <!-- Check all PHP files in the project -->
    <file>.</file>
    
    <!-- Exclude vendor and cache directories -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/cache/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*.cache</exclude-pattern>
    
    <!-- Use PSR-12 standard -->
    <rule ref="PSR12"/>
    
    <!-- Additional rules -->
    <rule ref="Generic.Arrays.DisallowLongArraySyntax"/>
    <rule ref="Generic.Formatting.SpaceAfterNot"/>
    <rule ref="Squiz.WhiteSpace.SuperfluousWhitespace"/>
    
    <!-- Allow some flexibility for existing code -->
    <rule ref="PSR12.Files.FileHeader.SpacingAfterBlock">
        <severity>5</severity>
    </rule>
    
    <!-- Maximum nesting level -->
    <rule ref="Generic.Metrics.NestingLevel">
        <properties>
            <property name="absoluteNestingLevel" value="5"/>
        </properties>
    </rule>
    
    <!-- Line length warning (not error) -->
    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="120"/>
            <property name="absoluteLineLimit" value="150"/>
        </properties>
        <exclude-pattern>*.md</exclude-pattern>
    </rule>
</ruleset>

